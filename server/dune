

(executable
 (name test)
 (libraries dream grid board core game yojson iolib core_unix.command_unix)
 (preprocess (pps ppx_jane lwt_ppx)))

(rule
 (targets test.ml)
 (deps test.eml.ml)
 (action (run dream_eml %{deps} --workspace %{workspace_root})))